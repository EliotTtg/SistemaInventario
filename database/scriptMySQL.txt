CREATE DATABASE IF NOT EXISTS sistema_inventario;
USE sistema_inventario;

-- =========================
-- TABLA ROLES
-- =========================
CREATE TABLE roles (
    id_rol INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(50) NOT NULL UNIQUE,
    descripcion VARCHAR(255)
);

-- =========================
-- TABLA CATEGORIAS
-- =========================
CREATE TABLE categorias (
    id_categoria INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL UNIQUE,
    descripcion TEXT
);

-- =========================
-- TABLA PROVEEDORES
-- =========================
CREATE TABLE proveedores (
    id_proveedor INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(150) NOT NULL,
    ruc VARCHAR(11) NOT NULL UNIQUE,
    telefono VARCHAR(15),
    direccion VARCHAR(255)
);

-- =========================
-- TABLA USUARIOS
-- =========================
CREATE TABLE usuarios (
    id_usuario INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
    usuario VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    id_rol INT NOT NULL,
    estado BOOLEAN DEFAULT TRUE,

    CONSTRAINT fk_usuario_rol
        FOREIGN KEY (id_rol)
        REFERENCES roles(id_rol)
);

-- =========================
-- TABLA PRODUCTOS
-- =========================
CREATE TABLE productos (
    id_producto INT AUTO_INCREMENT PRIMARY KEY,
    codigo VARCHAR(50) NOT NULL UNIQUE,
    nombre VARCHAR(100) NOT NULL,
    descripcion TEXT,
    stock_actual INT NOT NULL DEFAULT 0,
    stock_minimo INT NOT NULL DEFAULT 0,
    precio DECIMAL(10,2) NOT NULL,
    id_categoria INT NOT NULL,
    id_proveedor INT NOT NULL,

    CONSTRAINT fk_producto_categoria
        FOREIGN KEY (id_categoria)
        REFERENCES categorias(id_categoria),

    CONSTRAINT fk_producto_proveedor
        FOREIGN KEY (id_proveedor)
        REFERENCES proveedores(id_proveedor),

    CONSTRAINT chk_stock_actual
        CHECK (stock_actual >= 0),

    CONSTRAINT chk_stock_minimo
        CHECK (stock_minimo >= 0),

    CONSTRAINT chk_precio
        CHECK (precio >= 0)
);

-- =========================
-- TABLA TIPOS_MOVIMIENTO
-- =========================
CREATE TABLE tipos_movimiento (
    id_tipo_movimiento INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(50) NOT NULL UNIQUE,
    descripcion VARCHAR(255)
);

-- =========================
-- TABLA MOVIMIENTOS
-- =========================
CREATE TABLE movimientos (
    id_movimiento INT AUTO_INCREMENT PRIMARY KEY,
    fecha DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    cantidad INT NOT NULL,
    motivo VARCHAR(255),
    id_tipo_movimiento INT NOT NULL,
    id_producto INT NOT NULL,
    id_usuario INT NOT NULL,

    CONSTRAINT fk_movimiento_tipo
        FOREIGN KEY (id_tipo_movimiento)
        REFERENCES tipos_movimiento(id_tipo_movimiento),

    CONSTRAINT fk_movimiento_producto
        FOREIGN KEY (id_producto)
        REFERENCES productos(id_producto),

    CONSTRAINT fk_movimiento_usuario
        FOREIGN KEY (id_usuario)
        REFERENCES usuarios(id_usuario),

    CONSTRAINT chk_cantidad
        CHECK (cantidad > 0)
);

-- =========================
-- TABLA REPORTES
-- =========================
CREATE TABLE reportes (
    id_reporte INT AUTO_INCREMENT PRIMARY KEY,
    fecha_generacion DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    tipo_reporte VARCHAR(50) NOT NULL,
    id_usuario INT NOT NULL,

    CONSTRAINT fk_reporte_usuario
        FOREIGN KEY (id_usuario)
        REFERENCES usuarios(id_usuario)
);

-- =========================
-- INSERTAR ROLES
-- =========================
INSERT INTO roles (nombre, descripcion)
VALUES
('ADMINISTRADOR', 'Usuario con acceso total al sistema'),
('TRABAJADOR', 'Usuario encargado de movimientos de inventario');

-- =========================
-- INSERTAR TIPOS DE MOVIMIENTO
-- =========================
INSERT INTO tipos_movimiento (nombre, descripcion)
VALUES
('ENTRADA', 'Ingreso de productos al inventario'),
('SALIDA', 'Salida de productos del inventario');

-- =========================
-- INSERTAR CATEGORIAS
-- =========================
INSERT INTO categorias (nombre, descripcion)
VALUES
('Tecnologia', 'Productos tecnologicos'),
('Oficina', 'Utiles y productos de oficina');

-- =========================
-- INSERTAR PROVEEDORES
-- =========================
INSERT INTO proveedores (nombre, ruc, telefono, direccion)
VALUES
('Importaciones Lima SAC', '20123456789', '987654321', 'Av. Peru 123'),
('Distribuidora Norte', '20987654321', '912345678', 'Jr. Comercio 456');

-- =========================
-- INSERTAR USUARIOS
-- =========================
INSERT INTO usuarios (nombre, usuario, password, id_rol)
VALUES
('Juan Perez', 'admin', '123456', 1),
('Carlos Ruiz', 'trabajador1', '123456', 2);

-- =========================
-- INSERTAR PRODUCTOS
-- =========================
INSERT INTO productos (
    codigo,
    nombre,
    descripcion,
    stock_actual,
    stock_minimo,
    precio,
    id_categoria,
    id_proveedor
)
VALUES
('P001', 'Laptop Lenovo', 'Laptop Core i7', 10, 2, 3500.00, 1, 1),
('P002', 'Mouse Logitech', 'Mouse inalambrico', 25, 5, 80.00, 1, 1),
('P003', 'Cuaderno A4', 'Cuaderno cuadriculado', 50, 10, 12.50, 2, 2);

-- =========================
-- INSERTAR MOVIMIENTOS
-- =========================
INSERT INTO movimientos (
    cantidad,
    motivo,
    id_tipo_movimiento,
    id_producto,
    id_usuario
)
VALUES
(5, 'Ingreso inicial', 1, 1, 1),
(2, 'Venta de producto', 2, 1, 2),
(10, 'Reposicion de stock', 1, 2, 1);

-- =========================
-- INSERTAR REPORTES
-- =========================
INSERT INTO reportes (
    tipo_reporte,
    id_usuario
)
VALUES
('REPORTE_STOCK', 1),
('REPORTE_MOVIMIENTOS', 1);